# Telegram Bot Template

This repo implements a basic Telegram Bot infrastructure using Docker and python. It implements monorepo architecture with multiple services. Currently, the following services are implemented:
1. `tg_bot` - Telegram API interface
2. `backend` - Backend logic of the bot with a database
